const initialState = {
  positionHorizont: 0,
  positionVertical: 0,
  loading: false
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
